#pragma once

// Stable backend-owned path discovery / presentation contracts intentionally
// exposed to the CLI for discovery-only path reporting.

#include "Backend/Application/PathPresentationService.h"
#include "Backend/Scenario/PreparedPathStatus.h"
